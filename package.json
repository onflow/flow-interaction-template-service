{
    "name": "flow-templates",
    "version": "1.0.0",
    "description": "",
    "engines": {
        "node": ">=20.x"
    },
    "scripts": {
        "start": "cd api && node dist/src/index.js",
        "postinstall": "npx lerna exec npm install",
        "heroku-postbuild": "npx lerna exec npm run build",
        "build": "npx lerna exec npm run build",
        "vercel-build": "cd api && npm install && npm run build"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "lerna": "^8.1.8"
    }
}
